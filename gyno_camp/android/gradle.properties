org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Fix: Kotlin incremental compilation crashes when project (F:\) and Pub cache
# (C:\) are on different Windows drives. RelocatableFileToPathConverter cannot
# compute relative paths across drives, causing "Could not close incremental
# caches" AssertionError. Disabling incremental mode resolves this permanently.
kotlin.incremental=false
